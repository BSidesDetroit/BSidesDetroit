---
import * as HomePage from "../../content/home.md";
const stats = HomePage.frontmatter.stats;
---

<dl class="stat-strip">
  {
    stats.map((stat) => (
      <div>
        <dt>{stat.label}</dt>
        <dd>{stat.value}</dd>
      </div>
    ))
  }
</dl>
